---
title: 'Authentication'
description: 'How to authenticate with the Solarecliente API'
icon: 'key'
---

## Overview

The Solarecliente API uses API keys for authentication. You can generate and manage your API keys from the dashboard.

<Warning>
  Keep your API keys secure and never share them publicly. Treat them like passwords.
</Warning>

## Authentication Methods

### API Key Authentication

Include your API key in the request header:

```http
Authorization: Bearer YOUR_API_KEY
```

### Example Request

<CodeGroup>

```bash cURL
curl -X GET https://api.solarecliente.com/v1/clients \
  -H "Authorization: Bearer sk_live_abc123xyz789" \
  -H "Content-Type: application/json"
```

```javascript JavaScript
const response = await fetch('https://api.solarecliente.com/v1/clients', {
  method: 'GET',
  headers: {
    'Authorization': 'Bearer sk_live_abc123xyz789',
    'Content-Type': 'application/json'
  }
});

const data = await response.json();
```

```python Python
import requests

headers = {
    'Authorization': 'Bearer sk_live_abc123xyz789',
    'Content-Type': 'application/json'
}

response = requests.get(
    'https://api.solarecliente.com/v1/clients',
    headers=headers
)

data = response.json()
```

```php PHP
<?php

$headers = [
    'Authorization: Bearer sk_live_abc123xyz789',
    'Content-Type: application/json'
];

$ch = curl_init('https://api.solarecliente.com/v1/clients');
curl_setopt($ch, CURLOPT_HTTPHEADER, $headers);
curl_setopt($ch, CURLOPT_RETURNTRANSFER, true);

$response = curl_exec($ch);
$data = json_decode($response, true);

curl_close($ch);
```

</CodeGroup>

## API Key Types

<CardGroup cols={2}>
  <Card title="Test Keys" icon="flask">
    Used for development and testing. Prefix: `sk_test_`
  </Card>
  <Card title="Live Keys" icon="bolt">
    Used for production. Prefix: `sk_live_`
  </Card>
</CardGroup>

## Generating API Keys

1. Log in to your Solarecliente dashboard
2. Navigate to **Settings** > **API Keys**
3. Click **Generate New Key**
4. Select the key type (Test or Live)
5. Assign appropriate permissions
6. Copy and securely store your key

<Note>
  API keys are only displayed once upon creation. Make sure to save them securely.
</Note>

## API Key Permissions

You can configure granular permissions for each API key:

<Expandable title="Available Permissions">
  <ParamField path="clients" type="string">
    Access level for client resources: `read`, `write`, `delete`
  </ParamField>
  
  <ParamField path="projects" type="string">
    Access level for project resources: `read`, `write`, `delete`
  </ParamField>
  
  <ParamField path="installations" type="string">
    Access level for installation resources: `read`, `write`, `delete`
  </ParamField>
  
  <ParamField path="invoices" type="string">
    Access level for invoice resources: `read`, `write`, `delete`
  </ParamField>
  
  <ParamField path="reports" type="string">
    Access level for reports: `read`, `generate`
  </ParamField>
</Expandable>

## Security Best Practices

<CardGroup cols={2}>
  <Card title="Rotate Keys Regularly" icon="rotate">
    Update your API keys periodically for enhanced security
  </Card>
  <Card title="Use Environment Variables" icon="code">
    Store keys in environment variables, never in code
  </Card>
  <Card title="Restrict Permissions" icon="lock">
    Grant only the minimum required permissions
  </Card>
  <Card title="Monitor Usage" icon="chart-line">
    Review API key usage logs regularly
  </Card>
</CardGroup>

## Error Responses

### Missing Authentication

```json
{
  "success": false,
  "error": {
    "code": "UNAUTHORIZED",
    "message": "No API key provided"
  }
}
```

### Invalid API Key

```json
{
  "success": false,
  "error": {
    "code": "INVALID_API_KEY",
    "message": "The provided API key is invalid or has been revoked"
  }
}
```

### Insufficient Permissions

```json
{
  "success": false,
  "error": {
    "code": "FORBIDDEN",
    "message": "Your API key does not have permission to perform this action"
  }
}
```

## Revoking API Keys

To revoke an API key:

1. Go to **Settings** > **API Keys**
2. Find the key you want to revoke
3. Click **Revoke**
4. Confirm the action

<Warning>
  Revoking an API key immediately invalidates all requests using that key.
</Warning>
